
# 圖片存放資料夾 (Images Folder)

這裡是存放網站本地圖片的地方。

### 如何使用：
1. 將你的圖片（如 `my-banner.jpg`）放入此資料夾。
2. 在 `content.ts` 中，將圖片路徑設定為：`images/my-banner.jpg`

### 建議圖片規格：
- **Banner 背景圖**：寬度 1920px 以上，比例 16:9，建議壓縮後在 500KB 以下。
- **活動/新聞圖**：寬度 800px 以上，建議壓縮後在 200KB 以下。
- **建議格式**：.jpg 或 .webp (檔案較小)。
